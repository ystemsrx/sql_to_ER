# 让 pnpm 装上常用 peer，符合多数 React/Vite 项目预期
auto-install-peers=true
# 严格遵守 engines 字段（如果将来加了），避免 Node 版本错配
engine-strict=true
# pnpm v10 默认 strict-peer-dependencies=true 在大型生态下经常误报，
# 此项目体量小，暂时放宽以避免 install 失败
strict-peer-dependencies=false
# 降噪
fund=false
